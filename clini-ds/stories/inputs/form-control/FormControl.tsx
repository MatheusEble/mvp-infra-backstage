import React from "react";

import MUIFormControl, { FormControlProps, FormControlOwnProps } from "@mui/material/FormControl"

export const FormControl = (props: FormControlProps | FormControlOwnProps) => <MUIFormControl {...props} />;
