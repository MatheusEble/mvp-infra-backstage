import React from "react";

import MUITextField, { TextFieldProps } from "@mui/material/TextField";

export const TextField = (props: TextFieldProps) => <MUITextField variant="standard" {...props} />;
