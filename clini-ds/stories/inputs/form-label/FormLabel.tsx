import React from "react";

import MUIFormLabel, { FormLabelProps, FormLabelOwnProps } from "@mui/material/FormLabel"

export const FormLabel = (props: FormLabelProps | FormLabelOwnProps) => <MUIFormLabel  {...props} />;
