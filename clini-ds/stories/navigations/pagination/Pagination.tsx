import React from "react";
import MUIPagination, { PaginationProps } from "@mui/material/Pagination";

export const Pagination = (props: PaginationProps) => <MUIPagination {...props} />;
export type { PaginationProps };