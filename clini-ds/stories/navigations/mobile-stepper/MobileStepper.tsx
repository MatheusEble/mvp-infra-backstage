import React from "react";

import MUIMobileStepper, { MobileStepperProps } from "@mui/material/MobileStepper";

export const MobileStepper = (props: MobileStepperProps) => (<MUIMobileStepper {...props} />);