import React from "react";

import MUIBottomNavigation, { BottomNavigationProps } from "@mui/material/BottomNavigation";

export const BottomNavigation = (props: BottomNavigationProps) => (<MUIBottomNavigation {...props} />);