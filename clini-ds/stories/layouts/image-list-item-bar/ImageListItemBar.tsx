import React from "react";

import MUIImageListItemBar, { ImageListItemBarProps } from "@mui/material/ImageListItemBar";

export const ImageListItemBar= (props: ImageListItemBarProps) => (<MUIImageListItemBar {...props} />);