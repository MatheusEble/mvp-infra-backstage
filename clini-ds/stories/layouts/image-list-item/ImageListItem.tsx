import React from "react";

import MUIImageListItem, { ImageListItemProps } from "@mui/material/ImageListItem";

export const ImageListItem = (props: ImageListItemProps) => (<MUIImageListItem {...props} />);