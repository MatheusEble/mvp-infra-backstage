import React from "react";

import MUIListItemAvatar, { ListItemAvatarProps } from "@mui/material/ListItemAvatar";

export const ListItemAvatar = (props: ListItemAvatarProps) => (<MUIListItemAvatar {...props} />);