import React from "react";

import MUIListSubheader, { ListSubheaderProps } from "@mui/material/ListSubheader";

export const ListSubheader = (props: ListSubheaderProps) => <MUIListSubheader {...props} />;