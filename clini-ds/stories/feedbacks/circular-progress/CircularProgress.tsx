import React from "react";

import MUICircularProgress, { CircularProgressProps } from "@mui/material/CircularProgress"

export const CircularProgress = (props: CircularProgressProps) => <MUICircularProgress {...props} />;
